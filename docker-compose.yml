version: '3.8'

services:
  manual-search:
    build: .
    ports:
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - MANUAL_PAGES=${MANUAL_PAGES}
    volumes:
      - ./data:/app/data
    restart: unless-stopped 